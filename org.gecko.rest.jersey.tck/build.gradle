
def resolveTask = tasks.named("resolve.tck_jakarta") {
	outputBndrun = layout.buildDirectory.file("tck_jakarta.bndrun")
}

tasks.named("testOSGi") {
	bndrun = resolveTask.flatMap { it.outputBndrun }
}
